# Example credentials structure
# Copy this to your encrypted credentials: rails credentials:edit

google:
  client_id: YOUR_GOOGLE_CLIENT_ID_HERE
  client_secret: YOUR_GOOGLE_CLIENT_SECRET_HERE
  redirect_uri: http://localhost:3000/auth/google/callback

# Optional: OpenAI credentials if you haven't set them yet
openai:
  api_key: YOUR_OPENAI_API_KEY_HERE

# Optional: Telegram credentials if you haven't set them yet  
telegram:
  bot_token: YOUR_TELEGRAM_BOT_TOKEN_HERE