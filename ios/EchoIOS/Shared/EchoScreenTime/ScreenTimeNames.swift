import DeviceActivity
import ManagedSettings

public extension ManagedSettingsStore.Name {
    static let echoFocus = Self("echo.focus")
}

public extension DeviceActivityName {
    static let echoFocus = Self("echo.focus")
}
